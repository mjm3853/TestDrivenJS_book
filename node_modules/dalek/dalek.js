#!/usr/bin/env node

var fs = require('fs'),
    path = require('path');


var dalek = module.exports = fs.readFileSync(path.resolve(__dirname, 'dalek.ansi'), 'utf8');

if (!module.parent) {
  console.log(dalek);
}
